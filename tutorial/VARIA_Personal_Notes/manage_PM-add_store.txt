REM Sorry I want to store the order of work after installing:
REM DO NOT USE uv add etc... AND simultaniously uv pip install...!!!!!!!
REM manage_restarts.bat
REM Use this if you want uv sync to be the reliable installer.
call .venv\Scripts\activate.bat
REM pm use for example these packages
uv init
uv add "django==5.2.*" djangorestframework
uv add --dev black ruff
uv lock
uv sync --active
REM Is that correct?
REM creating a new .venv

REM AFTER RUNNING THAT CODE:
deactivate
rmdir /s /q .venv
uv venv ...
call activate
python -V
Pause

REM The correct “reset venv” recipe (Windows CMD)
REM Run this from your project root (the folder containing .venv and manage.py):
REM 
cd /d C:\Users\hansh\django_sandbox\rest-framework-tutorial
REM If a venv is active, deactivate it (this will error if none active—harmless)
deactivate
REM Delete the old venv folder
rmdir /s /q .venv
REM Recreate venv with exact Python
uv venv --python 3.12.11 .venv
REM Activate
call .venv\Scripts\activate.bat
REM Confirm
python -V